from sqlalchemy import create_engine

engine = create_engine('mysql+pymysql://root:password@localhost/vbi')

DB_Credential = {
    'username': 'sadheesh',
    'password': 'password',
    'server': 'localhost',
    'database': 'vbi'
}